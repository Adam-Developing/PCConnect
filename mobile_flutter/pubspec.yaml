name: mobile_flutter
description: A new Flutter project for PCConnect.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  retrofit: ^4.1.0
  json_annotation: ^4.8.1
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.7
  socket_io_client: ^2.0.3
  flutter_riverpod: ^2.4.9
  go_router: ^12.1.1
  intl: ^0.19.0
  connectivity_plus: ^5.0.2
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  retrofit_generator: ^8.0.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
